{
  "name": "wasm-c-string",
  "version": "1.0.1",
  "description": "Library for passing JavaScript strings in and out of WebAssembly modules written in C",
  "type": "module",
  "main": "wasm-c-string.js",
  "scripts": {
    "build": "node build/build.js",
    "build:test": "node build/build-test.js",
    "test": "npx mocha test/*.spec.js"
  },
  "devDependencies": {
    "mocha": "^9.1.2"
  },
  "files": [
    "wasm-c-string.js"
  ]
}
